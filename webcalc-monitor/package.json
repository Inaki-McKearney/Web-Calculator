{
  "name": "webcalc-monitor",
  "version": "1.0.0",
  "description": "API Response Monitor",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./index.js",
    "ui": "browser-sync start --proxy=localhost:8000 --files='**/*.css, **/*.pug, **/*.js' --ignore=node_modules --reload-delay 10 --no-ui --no-notify",
    "start": "node ./index.js"
  },
  "author": "Inaki McKearney",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "nodemon": "^2.0.1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "line-reader": "^0.4.0",
    "n-readlines": "^1.0.0",
    "pug": "^2.0.4",
    "request": "^2.88.0",
    "winston": "^3.2.1"
  }
}
